pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }
        stage('List all files'){
            steps {
                bat "node --version"
            }
        }
    }
}
